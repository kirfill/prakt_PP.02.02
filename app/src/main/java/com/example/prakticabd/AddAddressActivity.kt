package com.example.addaddress

import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {
//    override fun onCreate(savedInstanceState: Bundle?) {
//        super.onCreate(savedInstanceState)
//        setContentView(R.layout.activity_add_address)
//
//        val addressEditText = findViewById<EditText>(R.id.addressEditText)
//        val saveButton = findViewById<Button>(R.id.saveButton)
//
//        saveButton.setOnClickListener {
//            val address = addressEditText.text.toString()
//            if (address.isNotEmpty()) {
//                Toast.makeText(this, "Адрес сохранён: $address", Toast.LENGTH_SHORT).show()
//                // Здесь можно добавить логику для сохранения адреса
//            } else {
//                Toast.makeText(this, "Введите адрес", Toast.LENGTH_SHORT).show()
//            }
//        }
//    }
}
